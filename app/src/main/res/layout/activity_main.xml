<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/my_bg"
    tools:context=".MainAntivity">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_centerInParent="true"

        >
        <ImageView
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@drawable/logo_eat_it"
            android:layout_gravity="center"
            />

        <TextView
            android:id="@+id/IdTxtSlogan"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Una manera de satisfacer las ganas de comer"
            android:textColor="@android:color/white"
            />

    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:weightSum="2"
        android:orientation="horizontal"
        android:layout_alignParentBottom="true"
        >

        <info.hoang8f.widget.FButton
            android:id="@+id/IdSignUp"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Sign  up"
            android:layout_margin="8dp"
            android:textColor="@android:color/white"
            app:shadowEnabled="true"
            app:shadowHeight="4dp"
            app:cornerRadius="8dp"
            app:buttonColor="@color/btnSignUp"
            app:shadowColor="@android:color/black"
            />

        <info.hoang8f.widget.FButton
            android:id="@+id/IdSignIn"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Sign  In"
            android:layout_margin="8dp"
            android:textColor="@android:color/white"
            app:shadowEnabled="true"
            app:shadowHeight="4dp"
            app:cornerRadius="8dp"
            app:buttonColor="@color/btnSignActivo"
            app:shadowColor="@android:color/black"

            />


    </LinearLayout>

</RelativeLayout>